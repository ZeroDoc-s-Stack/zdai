# Required for claude-* model invocations
ANTHROPIC_API_KEY=sk-ant-...

# Required when provider=openrouter in zdai-state.json
OPENROUTER_API_KEY=sk-or-...

# Set these as environment variables on the host and pass via -e to podman run.
# Never bake secrets into the image or commit them to the repo.
