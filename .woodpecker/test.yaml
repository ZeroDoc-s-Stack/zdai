when:
  - event: push
    branch: [main, release, release/*]

matrix:
  platform:
    - linux/arm64

labels:
  platform: ${platform}

steps:
  - name: test
    image: docker.io/library/golang:1.25-alpine
    commands:
      - apk add git gcc musl-dev make
      - make build
      - go test ./...
